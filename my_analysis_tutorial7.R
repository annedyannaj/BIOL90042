# write some code 
my_vector <- 1:50
mean(my_vector)
hist(my_vector)

# type some stuff 
median(my_vector)

# make sure you save your file

# add some more code
# then add, commit and push these changes 
# blah blah blah
new_vector <- c("a", "b", "c")
toupper(new_vector)
